---
/**
 * TestimonialsSection Component - Apple-Inspired Light Pastel Theme
 *
 * Displays client testimonials and concrete business results to create social proof
 * and emotional momentum before the ContactSection.
 *
 * Features:
 * - 3 varied client testimonials (infopreneur, CEO B2B, formateur)
 * - Results-focused content with quantifiable metrics
 * - Responsive grid: 1 col mobile, 2 col tablet, 3 col desktop
 * - Semantic HTML with WCAG AA accessibility
 * - Inspirational tone to drive conversion
 *
 * Conversion Flow: "Wow résultats" → "Moi aussi je peux" → "Je veux ça" → ContactSection
 */

import TestimonialCard from '../ui/TestimonialCard.astro';
import GeometricBackground from '../ui/GeometricBackground.astro';

// Testimonials content - 3 varied client profiles with concrete results
const testimonials = [
  {
    result: 'Chiffre d\'affaires multiplié par 3',
    quote: 'Nous produisions des contenus performants en France. Make It Global les a adaptés pour les marchés anglophone et hispanophone. Six mois plus tard, notre chiffre d\'affaires avait triplé grâce à ces nouveaux marchés.',
    clientType: 'Dirigeant — Infoproduits',
    clientName: 'M.L.',
  },
  {
    result: 'Marché allemand ouvert en 30 jours',
    quote: 'Nous reportions notre expansion en Allemagne depuis deux ans. Avec Make It Global, nous avons transmis nos vidéos produits un lundi et reçu les versions allemandes le vendredi suivant. Premier client allemand le mois suivant.',
    clientType: 'CEO — PME industrielle B2B',
    clientName: 'P.D.',
  },
  {
    result: '60% de clients internationaux',
    quote: 'Mon activité de formation était cantonnée au marché français. Aujourd\'hui, plus de la moitié de mes clients sont anglophones ou hispanophones. La qualité du doublage a fait la différence.',
    clientType: 'Dirigeant — Organisme de formation',
    clientName: 'S.R.',
  },
];
---

<section
  id="testimonials"
  aria-labelledby="testimonials-heading"
  aria-label="Témoignages clients"
  class="relative py-20 md:py-32 px-6 md:px-12 bg-gradient-to-br from-primary-50/50 to-teal-50/30 overflow-hidden"
>
  <!-- Geometric Background Animation -->
  <GeometricBackground intensity="medium" variant="mixed" />

  <div class="relative z-10 max-w-7xl mx-auto">
    <!-- Section Header -->
    <div class="text-center mb-12">
      <h2 id="testimonials-heading" class="text-3xl md:text-4xl lg:text-5xl font-bold text-text-heading mb-4" data-reveal="fade-up">
        Ils ont accéléré leur développement international
      </h2>
      <p class="text-lg text-text-muted max-w-2xl mx-auto" data-reveal="fade-up" data-reveal-delay="100">
        Ces dirigeants ont choisi de déléguer la localisation de leurs contenus. Ils adressent aujourd'hui de nouveaux marchés.
      </p>
    </div>

    <!-- Testimonials Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8" data-reveal-children="200">
      {testimonials.map((testimonial) => (
        <div data-reveal="fade-up">
          <TestimonialCard
            result={testimonial.result}
            quote={testimonial.quote}
            clientType={testimonial.clientType}
            clientName={testimonial.clientName}
          />
        </div>
      ))}
    </div>
  </div>
</section>
