---
/**
 * HeroSection Component - Dark Theme with Geometric Animations
 * Mobile-first responsive design with accessibility features
 */
import WhatsAppButton from '../ui/WhatsAppButton.astro';
import GeometricBackground from '../ui/GeometricBackground.astro';
import { WHATSAPP_NUMBER, WHATSAPP_DEFAULT_MESSAGE } from '../../config';
---

<section
  class="relative w-full overflow-hidden animate-fade-in opacity-100 bg-dark-base"
  aria-label="Hero"
>
  <!-- Geometric Background Animation -->
  <GeometricBackground intensity="strong" variant="cyan" />

  <!-- Gradient overlay for depth -->
  <div class="absolute inset-0 bg-gradient-to-b from-cyan-500/5 to-transparent pointer-events-none z-5"></div>

  <!-- Content overlay with improved readability -->
  <div class="relative z-10 max-w-4xl mx-auto px-4 py-32 md:py-48 lg:py-64 text-center">
    <!-- Headline with improved contrast over background image -->
    <h1 class="text-4xl md:text-5xl lg:text-6xl font-extrabold text-white drop-shadow-lg leading-tight">
      Développez votre business à l'international sans lever le petit doigt
    </h1>

    <!-- Subheadline with improved contrast over background image -->
    <p class="text-lg md:text-xl text-white/95 drop-shadow-md mt-4 md:mt-6 max-w-2xl mx-auto leading-relaxed">
      On traduit et adapte tout votre contenu — vidéos, formations, PDFs, site web — dans les langues de vos futurs marchés. Vous continuez votre activité en France, on s'occupe du reste.
    </p>

    <!-- CTA -->
    <div class="mt-8 md:mt-10 flex justify-center">
      <WhatsAppButton
        phoneNumber={WHATSAPP_NUMBER}
        message={WHATSAPP_DEFAULT_MESSAGE}
        section="hero"
        variant="primary"
      />
    </div>
  </div>
</section>
